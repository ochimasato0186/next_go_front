import React from "react";

const DesktopHeader: React.FC = () => (
  <header style={{ width: "100%", background: "#2d3748", color: "#fff", padding: "16px 32px", fontSize: 20, fontWeight: "bold" }}>
    No Look for School（PC版ヘッダー）
  </header>
);

export default DesktopHeader;
